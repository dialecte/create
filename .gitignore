# Local-only inputs (bring your own XSD + dev wrapper)
xsd/
local/

# Build output
dist/

# Vendored wheels (downloaded via `npm run vendor`; re-fetched in CI/publish)
vendor/*.whl

# Node
node_modules/

# Python dev env
python/.venv/
python/.pytest_cache/
python/**/__pycache__/
python/*.egg-info/

# Scratch
.tmp/

# OS
.DS_Store
